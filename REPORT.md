# PM 세이프라인 — 로드뷰 위험도 모델·데이터셋 보고서

로드뷰 이미지로 사고 위험을 예측하는 **teacher 모델**과 그 **학습 데이터셋**을 정비한 결과를
정리한다. teacher는 지식 증류·IRL 파이프라인(§4.4)에서 도로 구간의 위험도를 매기는 역할을 한다.

## 요약

- 위험도 teacher를 참조 모델(ZenSVI perception ViT)과 동일한 구조로 재구현했다.
- 대조군에 고속도로가 섞여 있던 데이터 결함을 찾아 제거했다.
- 사고 심각도를 반영하도록 학습 손실을 설계했다(severity-weighted BCE).
- 대전·청주·세종 3개 지역(깨끗한 데이터) 기준 **검증 AUC 0.786, 5-fold 교차검증 0.741 ± 0.050**.
- **데이터셋 구축 → 모델 학습 → IRL 가중치 학습**까지 전 파이프라인 검증을 마쳤다.
- 검증 성능이 유의미해 데이터를 늘리면 신뢰도가 더 오를 것으로 보고, 사고 지점이 많은
  **서울·부산을 추가**하기로 했다.

---

## 1. 위험도 모델

### 참조 모델
설계 문서가 지목한 teacher는 ZenSVI 패키지의 **거리뷰 지각(perception) 모델** 중 "안전 인지"
차원이다. 이 모델의 실제 구현을 확인한 결과:

- **PyTorch(torchvision) 기반**이며 HuggingFace `transformers`/`AutoModel`을 쓰지 않는다.
- 백본은 `torchvision vit_b_16`(SWAG 사전학습, 입력 384×384) + 3단 Linear·ReLU 분류 헤드다.
- MIT **Place Pulse 2.0**(거리 사진에 대한 사람들의 "안전해 보이는 정도" 라벨)로 학습됐다.

### 주의: '안전해 보이는 정도' ≠ '사고 위험'
ZenSVI의 "안전" 점수는 **사람이 시각적으로 느끼는 안전감**이지 실제 교통사고 위험이 아니다.
따라서 본 프로젝트의 teacher는 이 모델을 그대로 쓰지 않고, **같은 ViT 백본 위에 새 헤드를
얹어 실제 사고 라벨(KoROAD/TAAS)로 파인튜닝**해 **사고 확률**을 예측하도록 만든다.

### 구현(`pm_safeline.models.pm_risk_vit`)
- `torchvision vit_b_16`(SWAG 사전학습, 384) 백본 + 3단 Linear·ReLU 위험 헤드.
- 데이터가 적어 **백본은 고정하고 헤드만 학습**한다(linear probe, 학습 파라미터 약 46만).
- 입력 전처리는 참조 모델과 동일하다(384 리사이즈 + ImageNet 정규화).

### 입력 형식
- 모델은 **한 번에 이미지 한 장**(384×384)을 받아 사고 확률 하나를 낸다.
- 한 지점은 도로 기준 4방향으로 촬영하며, **각 방향을 따로 넣어** 방향별 확률을 얻은 뒤
  **지점 단위로 평균**한다.

---

## 2. 학습 방식

### 출력: 사고 확률(이진) + 심각도 가중
위험도를 다중 클래스(심각도) 대신 **사고 확률(0~1)**로 낸다. 경로 위험 집계 공식이 확률을
입력으로 받고, 표본이 적어 다중 클래스는 불안정하기 때문이다. 대신 사고 심각도를 **손실
가중치**로 반영한다.

- 대조(사고 없음) = 1.0, 사고는 심각도별 가중(경상 1.0 / 중상 2.0 / 사망 3.0).
- 클래스 불균형은 `pos_weight`로, 심각도는 표본별 가중으로 함께 보정한다.

---

## 3. 데이터셋 품질

### 발견한 결함: 대조군에 고속도로 혼입
사고 지점(양성)과 짝지을 대조 지점(음성)을 도로망에서 뽑는데, 도로망에 고속도로·자동차전용
도로가 포함돼 있었다. 그 결과 청주·세종의 대조 지점 일부가 **고속도로**에 찍혔다. 사고 지점은
모두 시가지 도로이므로, 이대로 학습하면 모델이 "위험 vs 안전"이 아니라 **"고속도로 vs 시가지"**를
학습하는 왜곡이 생긴다.

### 조치
| 문제 | 조치 |
|---|---|
| 대조군에 고속도로 혼입 | 도로망에서 motorway·trunk(및 링크) 제외 — PM/이륜차 진입 불가 도로 |
| 사고 적은 지역에서 매칭 붕괴 | 사고 수에 맞춰 노출(exposure) 층화 구간 수 축소 |
| 타지역 좌표 오류 미검출 | 각 지점을 **자기 지역 경계**로만 검증(전 지역 합집합 아님) |
| 같은 지점의 연도별 중복 | 40m 이내 중복을 한 지점으로 병합(train/valid 누수 방지) |

조치 후 대조군의 고속도로 혼입은 **0건**으로 확인됐다.

---

## 4. 학습 결과 (대전·청주·세종, 정비된 데이터)

- 데이터: 사고 86지점 / 대조 258지점, 이미지 1,375장(학습 1,099 / 검증 276).
- 백본 SWAG 384, 헤드만 학습, severity-weighted BCE.

| 지표 | 값 |
|---|---|
| 검증 AUC (단일 분할) | **0.786** |
| 학습 AUC | 0.787 |
| 5-fold 교차검증 AUC | **0.741 ± 0.050** (fold: 0.79 / 0.65 / 0.75 / 0.74 / 0.77) |

- 학습 AUC ≈ 검증 AUC이므로 **과적합은 없다**.
- ROC-AUC 0.7~0.8은 "쓸 만한" 수준이다. 데이터 결함을 제거하기 전(고속도로 혼입·랜덤
  초기화) 예비 수치가 0.6대였던 것과 비교하면, **SWAG 사전학습 백본 + 정비된 데이터**로
  뚜렷이 개선됐다.
- 사고 지점이 86곳으로 아직 적어 교차검증 편차(±0.05)가 있다. 지역 확장이 신뢰도를 높이는
  가장 확실한 방법이다.

---

## 5. 데이터 확장 결정

3개 지역만으로도 검증 AUC 0.786(과적합 없음)이 나왔고, 표본이 늘어나면 교차검증 편차(±0.05)가
줄어 신뢰도가 더 오를 것으로 판단했다. 이에 사고 지점이 많은 **서울·부산을 추가**하기로 결정했다.

- 배포 목표가 전국이므로 광역시(서울·부산·대구·인천·광주·울산)의 KoROAD 지역 코드를 미리
  검증해 등록해 두었다.
- 사고 지점은 **연도별 중복을 병합**해 순수 지점 수로 계산한다(6개 광역시 원시 1,806 → 병합 후
  1,276).
- 이번 확장은 **서울·부산 + 기존 3지역**으로 사고 지점 약 1,000곳 규모다.

---

## 6. 파이프라인 검증 완료

데이터셋 구축부터 IRL 가중치 학습까지 **전 파이프라인이 검증됐다**.

- **데이터셋**: KoROAD 사고 지점 + OSM 도로망 + 노출 매칭 대조 샘플링 + 로드뷰 4방향 수집,
  품질 정비(고속도로 제거·중복 병합)까지 동작 확인.
- **모델 학습**: teacher(ViT) 학습·5-fold 교차검증 완료(검증 AUC 0.786 / 0.741 ± 0.050).
- **IRL**: 경로 위험 집계 → Bradley-Terry 가중치 학습이 합성 데이터에서 진값을 정확히 복원,
  라우팅 서버용 `PmCostWeights` 형태로 출력.

향후 확장 데이터로 재학습하면 신뢰도가 더 오를 것으로 기대되며, 학습된 가중치를 라우팅
서버(`PmCostWeights`)에 주입하면 서비스에 반영된다.
