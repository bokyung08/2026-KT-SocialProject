{
  "name": "pmsafeline",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:flutter": "node scripts/copy-flutter.mjs",
    "build": "npm run build:flutter && ait build",
    "preview": "vite preview",
    "deploy": "ait deploy"
  },
  "devDependencies": {
    "vite": "^8.2.0",
    "@apps-in-toss/devtools": "^3.0.2"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "latest"
  }
}
